---
obj:
  - IT
tags:
  - AI
  - template
prjfolder: tmpl-b24-php
prjurl: /docs/
ssum: Хронология того, что уже сделано — новое сверху. Краткий формат
---

# История изменений

> Хронология того, **что уже сделано** — новое сверху.
> Каждая запись = один осмысленный шаг (фича, фикс, деплой, решение).
> Файл **только дописывается**. Подробности любого шага всегда можно
> увидеть в git: `git log --oneline`, `git show <hash>`.
>
> Пиши коротко: «что изменилось» и «зачем», а не «изменён файл X».
>
> История первого проекта на этом движке (клиент «Альфа», `ap-pdir`) — в
> [prev-project/changelog.md](./prev-project/changelog.md).

## Формат записи

```
## ГГГГ-ММ-ДД — Заголовок шага
- Что сделали (по пунктам)
- Зачем / что это даёт
- Связанные решения: D-NNN (если есть)
```

---

<!-- Новые записи добавляй СВЕРХУ, сразу под этой строкой -->

## 2026-07-22 — Реальные цвета стадий сняты живым REST, один плейсхолдер поправлен
- Временный скрипт на сервере (`sudo -u www-data php …`, токены на диске
  принадлежат `www-data`) прогнал `crm.status.list` по всем 4 entityTypeId,
  удалён сразу после — см. D-004
- Из 20 сверенных стадий разошлась одна: Сделка `UC_MVRPFS` («Закрытие») —
  был плейсхолдер `#00a74c` (зелёный), реальный цвет `#fff300` (жёлтый).
  Исправлено в `www/api/dashboard-data.php`. Остальные 19 совпали 1:1
- Задеплоено (`bash deploy.sh`)

## 2026-07-22 — Install-flow пройден, приложение рабочее
- Пользователь зарегистрировал local-app на `ooordi.bitrix24.ru`, прислал
  `B24_CLIENT_ID`/`B24_CLIENT_SECRET` — вписаны в `env.php` на сервере по SSH
  (`php -l` подтвердил валидный синтаксис после правки)
- Install-flow пройден на портале, приложение открылось из левого меню
- Живое подтверждение работоспособности (скриншот от пользователя): Пульт
  руководителя показывает 2 реальные сделки (`<9990> ЖК Солнечный`,
  `<9991> ЖК Мечта`) с корректными кодами, названиями, стадией «Работа»,
  суммами контракта/остатка, счётчиками этапов и модулей по стадиям,
  индикатором срыва графика — все entityTypeId и UF-коды, перенесённые
  из `/source/RDI *.md` без REST-сверки (D-002), совпали с реальными полями
- Не проверено вживую: кнопка создания задачи (`task-create.php`), реальные
  цвета стадий (`crm.status.list`) — см. `docs/state.md`
- Связанные решения: D-002 (подтверждено), D-003

## 2026-07-22 — Первый деплой на rub24
- `bash deploy.sh`: rsync `www/` → `/var/www/b24/rdi-pdir/`, `env.php` создан
  автоматически с верными `APP_URL`/`APP_PATH`/`DATA_ROOT`
- Добавлен блок `/rdi-pdir` в `/etc/caddy/Caddyfile` на rub24 (по образцу
  `ap-pdir`), конфиг провалидирован (`caddy validate`) и перезагружен
  (`caddy reload`) — файл правился напрямую по SSH, он принадлежит `deploy`
- Прод отвечает: `curl` на `https://rub24.blackboxbegin.space/rdi-pdir/index.php`
  → `HTTP 403` — ожидаемо (session-gate REFERER-fence блокирует прямой заход
  вне портала/self-APP_URL), подтверждает что PHP-FPM и роутинг рабочие
- Осталось: зарегистрировать local-app на `ooordi.bitrix24.ru`, вписать
  `B24_CLIENT_ID`/`B24_CLIENT_SECRET` в `env.php` на сервере, install-flow

## 2026-07-22 — Бизнес-код переписан под сущности «АУП»
- Пользователь положил в `/source` актуальные RDI-документы с полными кодами
  полей (UF_CRM_*) и описанием бизнес-процессов — составлены самим
  разработчиком портала при создании смарт-процессов, не сняты живым REST
- Переписаны `www/api/dashboard-data.php` (entityTypeId 1038/1042/1050/1046,
  все UF-коды, стадии и их порядок/названия по `/source/RDI *.md`),
  `www/api/task-create.php` (entityTypeId сделки 1038, поле объекта
  `UF_TASK_OBJ_NAME` вместо `UF_AUTO_824402759720` первого клиента, hex-префикс
  привязки `T40e_`), `www/js/app.js` (ENTITY_TYPE_ID, DEAL_STAGES)
- Проверено: `php -l` на обоих PHP-файлах, `node --check` на app.js — синтаксис
  чист; grep по всему `www/` — не осталось ссылок на entityTypeId/UF-коды/
  коды стадий первого клиента («Альфа»)
- НЕ проверено живым REST (нет доступа до install-flow, см. следующий шаг):
  соответствие UF-кодов из `/source` реальным полям на портале, и цвета
  стадий (`color` в `DASHBOARD_*_STAGES`) — временно перенесены по смыслу
  с воронки первого клиента, см. D-002
- Связанные решения: D-001, D-002

## 2026-07-22 — GitHub-репозиторий привязан
- Пользователь дал ссылку на созданный репозиторий:
  `git@github.com:raccoonspain/rdi-pdir.git`
- Проверен SSH-доступ (`ssh -T git@github.com` — ключ `~/.ssh/id_ed25519`
  уже привязан к аккаунту `raccoonspain`), `origin` подключён по SSH
  (не HTTPS — в окружении нет сохранённых HTTPS-кредов)
- Локальная история (3 коммита, включая онбординг) запушена в `master`
- Оставшийся блокер перед деплоем (см. D-001): бизнес-код ещё под сущности
  первого клиента — нужны реальные UF-коды `ooordi.bitrix24.ru`

## 2026-07-22 — Онбординг пройден: «Пульт АУП»
- Разработчик положил в `/source` бизнес-контекст сущностей нового портала:
  `RDI Сделки/Этапы/Модули/Оплаты/Задачи.md` — entityTypeId 1038/1042/1050/1046
- Отвечены вопросы онбординга: название «Пульт АУП», портал `ooordi.bitrix24.ru`
  (найден в вебхуках `/source`), плейсмент LEFT_MENU (дефолт), cron не нужен
  (`www/bin/process.php` в проекте и так отсутствует — не переносился)
- Заполнено «О ПРОЕКТЕ» в `CLAUDE.md`, обновлены `docs/project-brief.md` и
  `docs/state.md`, создан `.onboarding-done`
- UF-коды полей первого клиента («Альфа») не переиспользуются — конкретные
  ID полей нужно переснять живыми REST-вызовами (`crm.item.fields`) под
  портал `ooordi.bitrix24.ru`, хотя entityTypeId уже известны из `/source`
- Дальше: `bash deploy.sh`, регистрация local-app на `ooordi.bitrix24.ru`,
  `B24_CLIENT_ID`/`SECRET` в `env.php`, install-flow

## 2026-07-22 — Старт проекта: тиражирование ap-pdir для второго клиента
- Проект создан не из чистого шаблона `tmpl-b24-php`, а как копия рабочего
  `ap-pdir` (тот же движок «Пульта»: Сделка → Этап → Модуль) — для второго
  клиента на том же VPS rub24, другой портал Б24, другие ID сущностей/полей
- Перенесено как есть: код `www/`, грабли Б24 `rules/`, слэш-команды журнала
- Не перенесено (архивировано в `prev-project/`, не потеряно): бизнес-контекст
  и журнал первого клиента (Альфа) — `source/prev-project/`,
  `docs/prev-project/{state,changelog,decisions,project-brief}.md`,
  `docs/prev-project/superpowers/` (спеки и планы фич первого проекта)
- `.onboarding-done` удалён, `docs/{state,changelog,decisions,project-brief}.md`
  заведены заново с чистого листа под нового клиента; git-история начата
  заново (`git init`), без коммитов ap-pdir
- Дальше: онбординг под нового клиента, `/source` от разработчика, GitHub-репо,
  деплой и привязка к новому порталу (см. docs/state.md)
