---
obj:
  - IT
tags:
  - AI
  - template
prjfolder: tmpl-b24-php
prjurl: /docs/
ssum: Хронология того, что уже сделано — новое сверху. Краткий формат
---

# История изменений

> Хронология того, **что уже сделано** — новое сверху.
> Каждая запись = один осмысленный шаг (фича, фикс, деплой, решение).
> Файл **только дописывается**. Подробности любого шага всегда можно
> увидеть в git: `git log --oneline`, `git show <hash>`.
>
> Пиши коротко: «что изменилось» и «зачем», а не «изменён файл X».

## Формат записи

```
## ГГГГ-ММ-ДД — Заголовок шага
- Что сделали (по пунктам)
- Зачем / что это даёт
- Связанные решения: D-NNN (если есть)
```

---

<!-- Новые записи добавляй СВЕРХУ, сразу под этой строкой -->

## 2026-07-16 — Доводка модалки «Создать задачу»: префилл названия + дефолтные параметры задачи
- Поле «Название задачи» в модалке (`openTaskModal()`, `www/js/app.js`)
  теперь предзаполняется кодом объекта сделки и тире (например `017-`)
  вместо пустой строки
- В `tasks.task.add` (`www/api/task-create.php`) зашиты два дефолтных
  параметра задачи: `ALLOW_CHANGE_DEADLINE = 'N'` (запрет исполнителю
  менять крайний срок) и `TASK_CONTROL = 'Y'` (проконтролировать задачу
  после завершения) — имена полей и Y/N-значения сверены по
  `tasks.task.get.fields` в `b24-rest-docs`, не угаданы
- Проверено локальным fixture-стендом (стаб `b24()`/`requireSession()`
  + `php -S`): оба поля подтверждённо уходят в тело REST-вызова.
  Задеплоено и подтверждено пользователем живьём в Б24
- Новые/изменённые файлы: `www/js/app.js`, `www/api/task-create.php`
- Связанные решения: D-015

## 2026-07-15 — Срыв срока на уровне Модуля: колонка «Лаг» и сводка «М +/-» у Этапа
- Новая чистая функция `dashboardModuleLagDays()` считает лаг модуля в
  днях (дата-план текущей стадии минус сегодня) по уже существующим
  полям-датам стадий (`UF_CRM_19_MOD_RUN/CHECK/CREATE/EDIT/WAIT/APPROVE`)
  — БП-автоматизации для этого не было, считаем в приложении
- `fetchDashboardData()` отдаёт `lagDays` у каждого модуля и агрегат
  `moduleLag: {onTrack, overdue, hasModules}` у каждого этапа
- UI: колонка «Лаг» в подтаблице Модулей, колонка «М +/-» в подтаблице
  Этапов (`N/M`, `БМ` при отсутствии модулей, `0/0` — валидный случай)
- По итогам живой проверки и финального ревью: фикс выравнивания
  заголовков `.num` в подтаблицах (задевал и старые колонки Цена/Дни
  КП-План), фикс защиты от невалидной/legacy-нулевой даты из CRM (могла
  уронить весь дашборд), фикс цвета для `lagDays === null`
- Новые/изменённые файлы: `www/api/dashboard-data.php`, `www/js/app.js`,
  `www/css/style.css`
- Реализовано через subagent-driven-development на ветке
  `worktree-srok-modulya` (спека
  `docs/superpowers/specs/2026-07-15-srok-modulya-design.md`, план
  `docs/superpowers/plans/2026-07-15-srok-modulya.md`), 3 задачи + фиксы
  по итогам финального ревью, все прошли implementer+reviewer цикл,
  ветка слита в `master` и задеплоена на прод
- Связанные решения: D-014

## 2026-07-14 — Кнопка 🎯 «Создать задачу» на строках Сделка/Этап/Модуль
- Кнопка на всех трёх уровнях аккордеона открывает модалку создания
  задачи Б24 (`tasks.task.add`): название, предзаполненное шаблонное
  описание («Задача к сделке/этапу/модулю «…»»), выбор исполнителя,
  срок
- Задача всегда привязывается к CRM-элементу Сделки (`UF_CRM_TASK`),
  независимо от уровня, на котором нажата кнопка; автоматически
  заполняется служебное поле задачи «Объект наименование»
  (`UF_AUTO_824402759720`) значением «Краткого наименования» сделки;
  тег `Задача из пульта`; постановщик — реальный пользователь,
  открывший приложение (не технический аккаунт local-app)
- Новые/изменённые файлы: `www/api/session.php` (userId в сессии),
  `www/api/dashboard-data.php` (objectShortName сделки + список
  активных сотрудников), `www/api/task-create.php` (новый эндпоинт),
  `www/js/app.js`/`www/template.html`/`www/css/style.css` (кнопка +
  модалка)
- Реализовано через subagent-driven-development на ветке
  `worktree-sozdanie-zadachi` (спека
  `docs/superpowers/specs/2026-07-14-sozdanie-zadachi-design.md`, план
  `docs/superpowers/plans/2026-07-14-sozdanie-zadachi.md`), 4 подзадачи,
  все прошли implementer+reviewer цикл, задеплоено на прод
- Связанные решения: D-013

## 2026-07-14 — Выравнивание колонок под-таблиц Этапов/Модулей
- Колонка «Стадия» (и остальные) под-таблиц Этапов/Модулей больше не
  скачет по горизонтали между разными раскрытыми блоками аккордеона —
  причина была в `table-layout: auto` по умолчанию, каждая
  `<table class="sub-table">` считала ширину колонок по своему
  содержимому независимо
- Модификаторы класса `sub-table--milestones`/`sub-table--modules`
  (`www/js/app.js`) + `table-layout: fixed` с процентными ширинами
  колонок, раздельно для двух форм таблицы (`www/css/style.css`)
- Выравнивание — только внутри своего уровня (все Этапы между собой,
  все Модули между собой); между уровнями ширина/отступ остаются
  разными — это намеренно, часть визуального языка вложенности (D-008)
- Реализовано напрямую в `master` (см. план
  `docs/superpowers/plans/2026-07-14-vyravnivanie-kolonok.md`),
  задеплоено, подтверждено пользователем глазами в Б24
- Связанные решения: D-012

## 2026-07-14 — Индикаторы срыва графика/оплаты — эмодзи вместо точек
- Колонка «Индикаторы»: цветные точки 🔴🟡 заменены на текстовые эмодзи
  ⏰ (срыв графика) / 💰 (ожидание оплаты) — по выбору пользователя,
  замена, а не добавление рядом с точками
- `title`-подсказка сохранена без изменений; `.indicator-dot`-CSS
  удалён как более не используемый (`www/js/app.js`, `www/css/style.css`)
- Зачем: точки были мелкими, жёлтая плохо отличима от зернистой текстуры
  фона таблицы — эмодзи сами по себе несут смысл, цвет для различения
  не нужен
- Реализовано напрямую в `master` (правка на 2 файла, без ветки/
  подагентов — см. план
  `docs/superpowers/plans/2026-07-14-indikatory-emodzi.md`), задеплоено,
  подтверждено пользователем глазами в Б24
- Связанные решения: D-012

## 2026-07-14 — Заказчик/Ответственный (колонки+фильтр) + цветные плашки счётчиков этапов/модулей
- В строке сделки — колонки «Заказчик» (`UF_CRM_13_CUSTOMER_COMP`,
  резолв через `crm.company.list`) и «Ответственный» (`assignedById`,
  резолв через `user.get`, тот же вызов что и для разработчиков
  модулей), плюс клиентские dropdown-фильтры по обоим полям в тулбаре —
  без новых REST, фильтр скрывает сделку целиком вместе с поддеревом
  этапов/модулей
- Счётчики «Этапы»/«Модули» — вместо текстовых лейблов («2 заверш ·
  1 ожид») теперь цветные плашки-числа, упорядоченные по стадии
  жизненного цикла, с подсказкой при наведении на читаемое название
  стадии (`dashboardOrderedStageCounts()`, `countBadges()`)
- По ходу найден и исправлен баг: поле «Заказчик Компания» на портале
  хранит значение CRM-привязки без документированного префикса
  (`"5"`, а не `"CO_5"`) — парсер обобщён на оба формата
- Реализовано через subagent-driven-development (план
  `docs/superpowers/plans/2026-07-14-zakazchik-otvetstvenny-filtr.md`):
  8 задач + 1 фикс, каждая прошла implementer+reviewer цикл; бэкенд
  проверен реальным REST на `alfa-prj.bitrix24.ru` до и после фикса,
  фронтенд — визуально пользователем в браузере после деплоя
- Зачем: пользователь попросил видеть заказчика/ответственного по
  сделке без открытия карточки в Б24, плюс почистить визуальный шум в
  колонках счётчиков (разъезд текста, плохая читаемость жёлтого на
  фоне)
- Связанные решения: D-011

## 2026-07-13 — KPI-плашки: число этапов в скобках рядом со сделками
- Плашки «Со срывом графика» и «Ожидают оплаты» теперь показывают
  `N (M)` — N сделок (как раньше) и M суммарно незавершённых этапов
  по всем сделкам, удовлетворяющих тому же условию, без раскрытия
  аккордеона
- Бэкенд: `fetchDashboardData()` считает `brokenScheduleMilestoneCount`/
  `awaitingPaymentMilestoneCount` по каждому незавершённому этапу
  независимо от стадии родительской сделки (см. D-010) — проверено
  фикстурой (скрипт-заглушка `B24`, не коммитится) на сценарии со
  скриншота пользователя (2(4)/1(1)) до и после правки
- Фронт: `template.html`/`app.js`/`style.css` — доп. `<span
  class="kpi-value-sub">` рядом с числом сделок, лейблы плашек уточнены
  («Сделок (этапов) со срывом графика» и т.п.)
- Связанные решения: D-010

## 2026-07-13 — Кнопки перехода на сущность в Б24 (Сделка/Этап/Модуль)
- В каждой строке таблицы (сделка, этап-Milestone внутри аккордеона, модуль
  внутри Milestone) рядом с названием добавлена иконка-стрелка ↗ —
  открывает карточку сущности в Б24 в новой вкладке
- URL строится по паттерну `crm/type/{entityTypeId}/details/{id}/`, домен
  портала берётся динамически через `BX24.getDomain()` при старте (без
  правок бэкенда — id и entityTypeId уже были в дереве данных)
- Клик по иконке не triggerит разворачивание/сворачивание строки —
  обработчик клика в `tbody` явно игнорирует `.entity-link`
- `www/js/app.js`, `www/css/style.css`
- Зачем: пользователь попросил быстрый переход со сводной страницы на
  конкретный объект в Б24, не листая дерево вручную
- Связанные решения: D-009

## 2026-07-13 — Визуальный рестайл под референс bugster.dev
- Пользователь показал bugster.dev (лендинг QA-тула, стиль «dev-tool
  стикербук»: зернистая бумага, чёрные обводки, моноширинный шрифт,
  квадратные «хендлы» по углам рамок) и попросил применить уместные
  элементы к пульту — без маскотов/пастельных стикеров, это рабочий
  инструмент, не лендинг
- Заменён точечный узор на настоящую зернистую SVG-текстуру
  (`feTurbulence`, инлайн data-URI, без внешних запросов) на тёплой
  бумажной подложке вместо серой
- KPI-плашки объединены в одну рамку с разделителями между колонками
  (было 4 отдельные карточки) — паттерн «10x / <5min / Zero» с bugster.dev
- Чёрные обводки (`--ink`) + увеличенный радиус у KPI-рамки и таблицы,
  квадратные хендлы по 4 углам (`.corner-handle`, HTML-разметка в
  `template.html` — `.handle-frame`-обёртка)
- Моноширинный шрифт (`--font-mono`) — заголовок страницы, KPI-числа,
  шапка таблицы, кнопки-пресеты; плотный текст в ячейках (названия,
  активность) оставлен sans-serif — moно на кириллице в длинных строках
  читается хуже
- Плашки стадий получили тонкую чёрную обводку — цвет заливки не
  трогали, он из реальной воронки Б24 (D-007), не бренд Bugster
- Не проверено глазами — нет браузера в этой сессии, отдано пользователю
- Связанные решения: D-008

## 2026-07-13 — Доводка UI по обратной связи: цена этапа, приглушённый фон таблицы
- В аккордеоне этапов добавлена колонка «Цена» (`UF_CRM_15_MST_COST`) между
  «Стадия» и «Дни КП-План» — не было в исходной спеке, добавлено по
  прямому запросу пользователя после просмотра живого UI
- Плашки стадий (чёрная/жёлтая/красная — реальные цвета из воронки Б24,
  см. D-007) слишком контрастировали на чистом белом полотне таблицы —
  добавлен мелкий точечный узор (`radial-gradient`, 14px, низкая
  непрозрачность) на фон таблицы сделок, аккордеона и вложенных таблиц
  этапов/модулей вместо плоского `#fff`; `--card-bg` тоже сдвинут на
  чуть тёплый нейтральный тон
- Проверено реальным REST-вызовом: `cost` этапа приходит корректно
  (например `017 m01 cost=6540000`)

## 2026-07-13 — Первая проверка глазами в Б24: работает; цвета стадий из воронки + фикс бага баннера
- Пользователь открыл пульт в Б24 — KPI, таблица, аккордеон, индикаторы
  отрисовались корректно на реальных данных (совпало с REST-проверкой)
- Найден и исправлен CSS-баг: пустой красный `error-banner` был виден
  поверх успешно загруженных данных — `display:flex` в правиле
  `.error-banner` перебивал атрибут `hidden` (равная специфичность,
  автор-стиль позже UA в каскаде); добавлено `.error-banner[hidden]{display:none}`
- По запросу пользователя цвета бейджей стадий (Deal/Milestone/Module)
  теперь не своя палитра, а точные цвета из воронок Б24 — сняты REST-вызовом
  `crm.status.list` (не подобраны на глаз со скриншота), захардкожены в
  `dashboard-data.php` вместе с названием стадии, эндпоинт отдаёт
  `stageColor`, фронт красит бейдж и выбирает чёрный/белый текст по
  формуле контраста — см. D-007
- Связанные решения: D-007

## 2026-07-13 — UI пульта: KPI, таблица, фильтры, аккордеон
- `www/template.html`, `www/js/app.js`, `www/css/style.css` — рабочий UI
  по контракту дизайн-спеки (вариант B): 4 KPI-плашки сверху, тулбар
  (поиск по коду/названию, чекбоксы 6 стадий, пресеты
  Активные/Все/Завершённые — пресет триггерит новый запрос к
  `dashboard.php`, поиск/чекбоксы — чистый клиентский фильтр по уже
  загруженным данным), сортируемая таблица (Код, Стадия — по бизнес-порядку),
  аккордеон сделка→этап→модуль без дополнительных REST при раскрытии,
  баннер ошибки с кнопкой «Повторить»
- В `fetchDashboardData()` добавлен резолв разработчиков модулей:
  `user.get` с `FILTER[ID]=[...]` одним REST-вызовом по всем уникальным
  `ufCrm19ModCreatorUser` → «Имя Фамилия» вместо голого числового ID
  (проверено на живых данных: id 1/19/23 → «Антон Изотов» / «Субподряд
  (СП)» / «Заказчик»)
- Не сделано (сознательно, см. state.md): точечная деградация при
  частичном REST-отказе (спека это описывает, дошли только до
  полноэкранного баннера); проверка глазами в браузере внутри Б24 — нет
  инструмента для этого в текущей сессии, отдано пользователю
- Связанные решения: D-004, D-006

## 2026-07-13 — Реализован fetchDashboardData() + эндпоинт dashboard.php
- `www/api/dashboard-data.php` — `fetchDashboardData()`: batch-загрузка
  `crm.item.list` (Deal 1050 → Milestone 1054 → Module 1062 → Pay 1058
  по стадии «Счёт отправлен», см. D-006), сборка дерева сделка→этапы→модули,
  подсчёт `lagDays`/индикаторов (🔴 срыв графика, 🟡 ожидание оплаты) и
  KPI-сводки по контракту из дизайн-спеки
- `www/api/dashboard.php` — тонкий эндпоинт: session-gate + `?filter=active|all|closed`
- По ходу TDD-проверки реальным REST-вызовом на живом портале обнаружено
  и задокументировано: `crm.item.list` `select`/ответ — camelCase-имена
  полей (`ufCrm13OCode`, не `UF_CRM_13_O_CODE`), верхний регистр в
  `select` молча теряет поле — новое правило
  `rules/rule-crm-item-camelcase-select.md`; плюс два уточнения архитектуры
  из спеки (Pay для 🟡-индикатора, постфильтр пресета в PHP) — см. D-006
- Проверено: 6 реальных сделок портала `alfa-prj.bitrix24.ru`, ручной
  пересчёт `lagDays`/индикаторов/KPI по сырым данным Milestone/Module
  совпал с выводом функции для пресетов `active`/`all`/`closed`;
  `dashboard.php` без сессии корректно отдаёт 403, `php -l` — без ошибок
- Осталось: UI (`www/js/app.js` + `www/template.html`) — KPI-плашки,
  таблица сделок, фильтры/сортировка, аккордеон
- Связанные решения: D-004, D-006

## 2026-07-13 — Install-flow на rub24 завершён
- Первая попытка «Переустановить» после правки Handler/Install URL в карточке
  local-app не долетела до сервера вообще (0 запросов в логе Caddy на rub24
  за 40 минут) — правка URL в форме не была явно сохранена кнопкой
  «Сохранить» перед «Переустановить»
- После явного сохранения формы install прошёл штатно:
  `alfa-prj.bitrix24.ru/marketplace/app/37/?install_finished=Y` →
  «BX24: OK», «Сессия: OK»
- Токены на сервере подтверждены (`domain: alfa-prj.bitrix24.ru`,
  `installFinishedUsers: [1]`, `installFinishedAt` проставлен)
- Зачем: без этого шага `$b24->call(...)` бросает исключение «токенов нет» —
  теперь можно делать реальные REST-проверки через `api/debug.php` и
  переходить к TDD-реализации `dashboard.php`

## 2026-07-13 — Деплой перенесён на VPS rub24 (минуя b24.blackboxbegin.space)
- Первый деплой на `b24.blackboxbegin.space` не завершился: Caddyfile там
  требует root для правки, install-flow не мог доехать до `index.php`
- По решению остановиться и повторить схему `ap-pp` (см. D-009 в его
  decisions.md): `deploy.sh` переписан на SSH-деплой на
  `deploy@45.91.55.178:/var/www/b24/ap-pdir/` (rub24.blackboxbegin.space)
- На rub24: сгенерирован `env.php`, вписаны `B24_CLIENT_ID`/`SECRET`,
  добавлен `handle /ap-pdir*` в Caddyfile (там он без root, правится по
  SSH напрямую), reload — маршрут отвечает штатным 403-guard'ом
- `CLAUDE.md` и `how-to-link.md` обновлены под новый сервер; локальный
  артефакт `/var/www/b24/ap-pdir` на исходной машине удалён (недостижим)
- Зачем: rub24 — целевой сервер проекта (ниже задержка до портала,
  проще эксплуатация Caddy); раз `ap-pdir` ещё не был живым, дешевле
  сразу выбрать конечный сервер, чем сначала поднимать на старом
- Осталось: обновить handler/install URL в карточке local-app в Б24 на
  `rub24.blackboxbegin.space` и повторно открыть приложение для install-flow
- Связанные решения: D-005

## 2026-07-13 — Утверждена дизайн-спека пульта (вариант B: таблица + KPI-сводка)
- Собрана и утверждена спека `docs/superpowers/specs/2026-07-13-pult-rukovoditelya-design.md`:
  макет главной страницы, состав колонок таблицы сделок, аккордеон
  Сделка → Этап → Модуль, правила индикаторов (🔴 срыв графика, 🟡 ожидание
  оплаты), фильтры (поиск, стадии, пресеты Активные/Все/Завершённые),
  сортировка, обработка ошибок REST
- Зафиксирована архитектура загрузки данных: один эндпоинт `www/api/dashboard.php`,
  батчами тянет `crm.item.list` (entityTypeId 1050/1054/1062), агрегирует
  на сервере в `fetchDashboardData()` (`api/lib.php`) и отдаёт фронту готовое
  дерево одним JSON — раскрытие строк на фронте без новых REST-вызовов
- Зачем: реализация UI/бэкенда пульта не начата без выбранного макета и
  зафиксированного контракта данных — это следующий шаг
- Связанные решения: D-004

## 2026-07-04 — env.php генерируется deploy.sh, init.php — fallback для 1%-кейса
- `deploy.sh` при первом деплое slug'а сам создаёт `env.php` из `env.example`
  с подставленными `APP_URL`/`APP_PATH`/`DATA_ROOT` (детерминированы конвентом
  VPS+Caddy); человеку остаётся вписать только `B24_CLIENT_ID`/`SECRET`
- `init.php` оставлен в шаблоне без изменений, но выведен из основного flow —
  теперь это инструмент для хостинга, где путь/домен заранее не известны
- Новый файл `how-to-link-ALL.md` — процедура привязки к Б24 для этого
  1%-кейса (через `init.php`, с ручной правкой `APP_URL`, если нужно)
- `how-to-link.md`, `README.md`, `CLAUDE.md`, `docs/state.md`, `docs/onboarding.md`
  обновлены под новый (99%-кейс) flow
- Зачем: задокументированный flow `deploy.sh → открыть init.php` не
  воспроизводим как описано — `deploy.sh` исключает `init.php` из rsync,
  на свежем сервере файла там просто нет. На практике оба живых проекта
  на VPS обошли это, написав `env.php` вручную — то есть шаг был лишним
  и вдобавок нерабочим
- Связанные решения: D-003

## — — Старт проекта
- Создан проект из шаблона `tmpl-b24-php`
- Подключена система журналирования: `docs/{handoff,state,decisions,changelog}.md`
- Дальше: деплой `www/` → `init.php` → создание local-app в Б24
