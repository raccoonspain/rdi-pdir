---
obj:
  - IT
tags:
  - AI
  - template
prjfolder: tmpl-b24-php
prjurl: /docs/
ssum: Состояние проекта — где мы сейчас. Живой снимок. Этот файл всегда отражает текущее положение дел.
---
# Состояние проекта — где мы сейчас

> **Живой снимок.** Этот файл всегда отражает текущее положение дел.
> Его не дописывают вниз, а **перезаписывают** — здесь только «сейчас»,
> вся история — в [changelog.md](./changelog.md).
> Обновляй после каждого осмысленного шага и **коммить**.

**Последнее обновление:** 2026-07-22
**Фаза:** Установлено и работает. «Пульт АУП» задеплоен на rub24, local-app
зарегистрирован на `ooordi.bitrix24.ru`, install-flow пройден, Пульт
руководителя рендерит реальные данные (2 сделки, этапы, модули).

---

## Где мы сейчас

Приложение «Пульт АУП» установлено на портале `ooordi.bitrix24.ru` и рабочее
— пользователь подтвердил скриншотом: Пульт руководителя открылся из левого
меню, показывает 2 активные сделки (`<9990> ЖК Солнечный`, `<9991> ЖК Мечта`)
с реальными кодами, стадией «Работа», суммами контрактов/остатков, счётчиками
этапов и модулей по стадиям, индикатором срыва графика. Это живое
подтверждение, что entityTypeId и все UF-коды, перенесённые из `/source/RDI
*.md` без предварительной REST-сверки (см. D-002), совпали с реальными
полями на портале — иначе поля были бы пустыми/null, а не заполненными
реальными значениями.

Код (`www/`), инженерные решения и грабли Б24 (`rules/`) перенесены из
`ap-pdir` как есть — архитектура и REST-паттерны те же (тот же движок
«Пульта»: Сделка → Этап → Модуль). Бизнес-контекст сущностей портала «АУП» —
в `/source/RDI *.md`: Сделка (entityTypeId=1038), Этап/Milestone (1042),
Модуль (1050), Оплаты (1046).

История первого проекта (для клиента «Альфа», портал `alfa-prj.bitrix24.ru`)
— в [docs/prev-project/](./prev-project/) (`state.md`, `changelog.md`,
`decisions.md`, `superpowers/`) и в `/source/prev-project` (бизнес-контекст
сущностей CRM того портала). Из этой истории стоит взять инженерный подход
(TDD с живыми REST-проверками, camelCase у `crm.item.list`, hex-префикс CRM-
привязки для смарт-процессов, батчи вместо foreach), но **не** конкретные
ID/коды полей.

Плейсмент — LEFT_MENU (дефолт, уже настроен в `www/api/bind.php`). Cron не
нужен (`www/bin/process.php` в проекте отсутствует — не переносился).

## Сейчас в работе

Приложение установлено и работает, реальные цвета стадий сняты и поправлены
(см. D-004). Найден один баг живой проверкой кнопки «создать задачу»:

```
tasks.task.add: The request requires higher privileges than provided by the access token
```

Причина известна — известная грабля Б24, см.
`rules/rule-b24-scope-add-needs-token-refresh.md`: карточка local-app на
`ooordi.bitrix24.ru` зарегистрирована со скоупами `crm, user, placement`
(минимум по онбордингу, см. CLAUDE.md) — `task` туда не входил, хотя
`task-create.php` (перенесённая фича «Пульта») вызывает `tasks.task.add`.
Даже после добавления scope в карточку — уже выданный `access_token` его не
подхватит без форс-обновления через `refresh_token`.

**Нужно от пользователя:** зайти в карточку local-app на портале
(Разработчикам → Другое → Локальное приложение → права доступа) и добавить
право **«Задачи» (task)**, сохранить карточку.

## Следующие шаги

- [x] Переписать `www/api/dashboard-data.php`/`task-create.php`/`www/js/app.js`
      под сущности «АУП» — сделано 2026-07-22
- [x] Получить ссылку на GitHub-репозиторий, привязать `origin` — сделано 2026-07-22
- [x] `bash deploy.sh` на rub24 (slug `rdi-pdir`), завести Caddy-блок `/rdi-pdir`
      — сделано 2026-07-22
- [x] Зарегистрировать local-app на портале `ooordi.bitrix24.ru`, вписать
      `B24_CLIENT_ID`/`SECRET` в `env.php` на сервере — сделано 2026-07-22
- [x] Пройти install-flow — сделано 2026-07-22, Пульт руководителя открылся
      и показал реальные данные (2 сделки)
- [x] Снять реальные цвета стадий `crm.status.list` — сделано 2026-07-22,
      один плейсхолдер поправлен (Сделка `UC_MVRPFS`), см. D-004
- [ ] Пользователь добавляет scope `task` в карточку local-app на портале
- [ ] После этого — форс-обновление `access_token` через `refresh_token`
      (временный скрипт, как в D-013 первого проекта / D-004 здесь), чтобы
      новый scope подхватился без переустановки
- [ ] Повторно проверить кнопку «создать задачу» — баг закрыт, только когда
      сценарий, который его вызвал, пройдёт без ошибки

## Открытые вопросы / блокеры

Один некритичный баг (не блокирует остальной функционал Пульта): создание
задачи падает из-за нехватки scope `task` — ждём от пользователя добавления
права в карточку local-app на портале.

## Карта проекта

| Что | Где |
|-----|-----|
| Точка входа (handler) | `www/index.php` |
| OAuth, токены | `www/api/b24.php` |
| Файловый store | `www/api/store.php` |
| Плейсменты | `www/api/bind.php` |
| Admin-gate | `www/api/session.php` |
| Данные пульта (агрегация, ID/поля первого клиента — нужно переснять) | `www/api/dashboard-data.php` |
| Эндпоинт пульта | `www/api/dashboard.php` |
| Эндпоинт создания задачи | `www/api/task-create.php` |
| UI-шаблон | `www/template.html` |
| JS-фронтенд | `www/js/app.js` |
| Install-flow диаграмма | `docs/install-flow-diagram.md` |
| Бизнес-контекст нового клиента (ждём от разработчика) | `/source` |
| История и бизнес-контекст первого клиента (Альфа) | `/source/prev-project`, `docs/prev-project/` |
| Прод URL | https://rub24.blackboxbegin.space/rdi-pdir/ |
| Хостинг / slug | VPS rub24.blackboxbegin.space (45.91.55.178), slug: rdi-pdir, деплой по SSH |
