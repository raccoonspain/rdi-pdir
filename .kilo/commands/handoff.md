---
description: Собрать сводку состояния для передачи проекта
agent: plan
---

Проект передают другому исполнителю. Подготовь так, чтобы новый исполнитель за 5 минут
понял «что, где и куда».

1. Сверь `docs/state.md` с реальным положением дел, обнови при расхождении.
2. Заполни блок «Снимок передачи» в конце `docs/handoff.md`: дата, суть проекта в одном
   предложении, что работает, что в процессе, прод URL/slug (b24.blackboxbegin.space),
   главные подводные камни (B24 OAuth, install-flow, Caddy uri strip_prefix), первый шаг
   для нового исполнителя.
3. Убедись, что всё закоммичено: `bash scripts/snapshot.sh "handoff: снимок передачи на $(date +%Y-%m-%d)"`.
4. Покажи итоговый «Снимок передачи».
